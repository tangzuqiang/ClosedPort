# Screenshots

Place the following PNGs here so README links resolve:

- main-flat.png      — Main window, flat view (default)
- main-grouped.png   — Main window, "Group by EXE" view
- folder-locks.png   — Folder lock scanner tab (Windows)
- floating.png       — Always-on-top mini panel

To capture them: run `npm start`, position the window, and use any
screenshot tool (Win+Shift+S on Windows, Shift+Cmd+4 on macOS,
PrtSc on Linux).
